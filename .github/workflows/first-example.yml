name: My first workflow

on: push

jobs:
    first_job:
        runs-on: ubuntu-latest
        steps:
        - name: go to root directory
          run: pwd

        - name: list the files
          run: ls -lrt
